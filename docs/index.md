# Welcome to Laravel Docs

This is the documentation for **My Laravel Project**.

## Installation

1. Clone the repo
2. Run `composer install`
3. Copy `.env.example` to `.env`
4. Run `php artisan key:generate`

## Routes

You can find all routes by running:

```bash
php artisan route:list